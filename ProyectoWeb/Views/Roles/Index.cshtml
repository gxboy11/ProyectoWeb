@model IEnumerable<ProyectoWeb.Models.Roles>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre_rol)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion_rol)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre_rol)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion_rol)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_rol">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_rol">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_rol">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
